# proofolio

새 에이전트가 작업을 이어받을 때는 [agent.md — 현재 기능·구현·검증·주의사항](agent.md)을 먼저 읽는다.

> **2026-09-20 — PR #2를 최신 main 기능 위에 통합.** 기존 `/`의 이메일 로그인·분석·예제·내 기록·답변 복구와 OpenRouter 코어, Vercel 설치 설정을 유지한다. PR의 화면은 `/dashboard`(내 채용 테스트)·`/test`(코드로 응시)·`/tests/:id`(내 테스트 결과)에 추가했다. 로컬 목계정/JSON 저장/가짜 AI 채점을 제거하고 같은 Supabase Auth와 기존 질문·답변에 연결했다. **SQL 003 적용 및 실제 DB 합성 왕복 확인**, 새 유료 모델 호출 0회. 플랫폼 전체 관리자나 자동 답변 평가는 아니다. [새 화면과 접근 계약](supabase/README.md#채용-테스트-pr-2).

> **2026-09-20 — Supabase 회원·저장·예제 연결 완료.** 이메일·비밀번호 로그인, 문항별 DB 저장/재시도, 새로고침 복구, 내 기록, 비공개 원문/이미지 보관을 연결했다. 비회원은 기존 디자인 **7개**·마케팅 **9개**·코딩 **7개** 예제를 읽기 전용으로 체험한다. 목표 **6~10개**, 예상 시간 **약 8분**(보장 아님). 실제 프로젝트에 SQL 001/002와 예제를 적용했다. [설정·검증·남은 일](supabase/README.md). **메일 발송 설정/실제 수신 확인은 사용자 요청으로 보류**, Google 로그인·관리자 화면은 제외했다.

> 이번 작업의 새 모델 호출은 코딩 예제 **1회, $0.0461403**뿐이다(입력 22,333 / 출력 2,369 / 보고된 추론 0 토큰, 약 19.34초, 재시도 0). 디자인/마케팅은 기존 결과를 그대로 가져왔고 유료 재분석하지 않았다. 코딩은 경로·원문 구절 연결만 검사하며 의미·보안·코드 실행 검수는 하지 않았다.

> **2026-09-20 — Gemini 재시험 2쌍 완료, 아직 최소 MVP 미달.** 최신 디자인 **자동7/별도 적합6**, 마케팅 **자동9/명확한 적합8(용어 1개 보류)**. 콘셉트·판단 기준을 묻는 질문은 복구됐지만 핵심 설명 누락·일부 전제 문제가 남았다. 이번 4회 실제 비용 **$2.91097905**. [질문·원인·토큰·시간·비용 보고서](benchmark/GEMINI_BROAD_RETEST_REPORT.md). Codex 원문 검수이며 사람 전문가 평가는 아니다.

> 질문은 프로젝트·작업물의 목적, 전체 접근, 핵심 판단, 결과 해석을 우선한다. 작은 시각 요소의 명시나 답변의 사전 기록은 요구하지 않는다. 없는 역할·수행·성과의 단정과 잘못된 출처는 계속 거부하며, 넓은 질문의 채택과 핵심 포인트 충족을 별도로 기록한다. Gemini→Opus→Gemini 기본 경로 유지.

> **이전 이력: Astra→Sonnet→Opus 단일 시험 중단.** 첫 프로젝트 근거 4개 중 Opus 자동 통과 2개, 정상 근거 과잉 제외 2개와 후속 좌표 오류를 확인했다. Sonnet 질문 생성에는 도달하지 않아 새 조합의 질문 품질은 미확인이다. 약 9분 37초, 확인 비용 **$3.08942750** + 마지막 취소 요청 비용 미확인. 이 조합은 재실행하지 않았고 이후 위 Gemini 경로로 재시험했다. [단일 시험 기록](benchmark/ASTRA_SINGLE_TRIAL_REPORT.md).

> 더 이전 0.17.1 결과는 디자인 **자동9/별도 적합4**, 마케팅 **자동3/별도 적합2**였다. [당시 실행·비용·한계](benchmark/MVP_VALIDATION_REPORT.md)는 보존한다. 코딩 1회까지 USD23 승인 창의 확인 누적 지출은 **$6.04654685**, 미확인 과거 비용 유보는 **$1.67344**, 가용 잔여는 **$15.28001315**다(2026-09-20 스냅샷, 계정 잔액 아님). 활성 유료 요청은 없고 미리보기 예산은 **0**이다. 과거 별도 원장은 변경하지 않았다.

> v0.17 첫 전체 CLI 시험에서 명시적 429를 실제로 한 번 만나 비용 확인→30초 대기→재시도 복구했다. 공급자 내부 재시도는 미확인이고 429 문제가 일반적으로 해결됐다는 뜻은 아니다. [이전 웹 이력](benchmark/TEN_QUESTION_WEB_REPORT.md) · [재개 위치](CHECKPOINT.md).

포트폴리오 PDF를 읽고 **제출물의 근거를 연결한 질문**을 만드는 로컬 프로토타입.
Node.js 24 + TypeScript 코어 **0.17.5**, 결과 스키마 **0.17**, Next.js UI(PR #1)를 함께 사용한다.
디자인·마케팅 PDF와 간단한 공개 GitHub 코드 질문을 지원한다. 실제 저자·성과의 진위, 지원자의 신뢰도·채용 합불은 판정하지 않는다.

## 현재 구성

웹·기본 CLI 실행 경로는 다음과 같다. 별도 명시적 단일 모델 시험은 위 보고서에 구분했다.

`웹 또는 CLI → OpenRouter Gemini 비전 → 검증 근거 JSON → OpenRouter Claude Opus 질문 → Gemini 원본 대조`

- 회원 UI: 이메일 로그인 → 직무 선택 → PDF/공개 GitHub 입력 → 분석 → 실제 생성된 질문 → 문항별 Supabase 저장 확인. 비회원은 DB에 저장된 예제만 체험한다.
- 모델: 스캔 `google/gemini-3.8-flash`, 시각 추출·검수 `google/gemini-3.1-pro-preview`, 질문 `anthropic/claude-opus-5`.
- 분석 기본값: 최대 5페이지·3개 검증 포인트·2개 프로젝트·10문항. 필수 맥락 페이지가 예산에 맞지 않으면 포인트 전체를 보류한다.
- 기본·최대 수는 `src/constants.ts`의 10, 회원 웹 요청 범위는 **6~10**이다. 기존 코어/CLI의 1~10 호환은 유지한다. 근거가 부족하면 적게 반환하며 0개는 시작을 막는다. PDF 질문 생성은 최대 2라운드다.
- 코딩: 공개 GitHub 저장소의 커밋을 고정해 허용된 텍스트 파일 최대 8개/합계 120KB를 Flash에 한 번 전달한다. 코드를 실행하지 않으며 정확한 파일·원문 구절이 연결된 질문만 표시한다.
- Gemini Developer API, Files API, 직접 GCP/ADC 클라이언트와 이전 유료 재개·질문 재실행 스크립트는 제거했다. Gemini **모델**은 OpenRouter를 통해 계속 사용한다.
- 과거 PDF·결과·질문·비용 원장·검증 보고서는 보존한다. 과거 보고서의 실행 명령과 가격은 당시 기록이며 현재 실행 경로가 아니다.

## 설치·환경 설정

```sh
node --version                 # 24.x
npm ci
npm ci --prefix web
cp -n .env.example .env        # 기존 .env는 덮어쓰지 않는다
```

[.env.example](.env.example)을 참고해 **루트** `.env`에 `OPENROUTER_API_KEY`를 설정한다.
기존 `.env.openrouter`도 읽는다. 우선순위는 프로세스 환경변수 → `.env` → `.env.openrouter`다.
Gemini API 키·Google ADC는 필요하지 않으며 레거시 자격증명은 로더가 읽지 않는다.
실제 키·원본 PDF·실행 결과는 Git에 포함하지 않는다. `NEXT_PUBLIC_` 변수에 키를 넣지 않는다.

`PROOFOLIO_MAX_COST_USD=0`은 **유료 실행 금지**다. 웹에서 실제 분석할 때만 승인된 누적 한도(0 초과 10달러 이하)를 명시한다.
CLI의 `--max-cost-usd`는 같은 한도를 명시하는 대안이다. 이미 만든 원장의 최초 한도와 동일해야 한다.
`PROOFOLIO_BUDGET_LEDGER=output/openrouter-budget.jsonl`을 CLI·웹·벤치마크가 함께 사용한다.
한도 부족을 해결하려고 새 원장으로 바꾸거나 원장을 삭제하지 않는다.
별도 명시적 운영자 승인이 있는 경우에만 기존 원장에 추가 지출 승인 행을 덧붙일 수 있다. 기본 `output/openrouter-budget.jsonl`은 과거 비용 미확인으로 차단된 원장이며 자동 해제하지 않는다. 이후 별도 승인된 USD23 시험 원장의 내역은 상단/체크포인트에 구분한다. 이번 작업에서 웹의 유료 실행 설정을 켜거나 새 원장을 만들지 않았다. 공개 운영 전 사용할 승인 원장·한도를 명시적으로 결정해야 한다.

무료 키·모델·가격·ZDR 확인:

```sh
npm run check:openrouter
```

이 검사는 질문 생성이나 PDF 전송을 하지 않는다. 실제 생성 시에는 리셋 없는 유한 OpenRouter 키 한도가 필요하며,
`google-vertex/global`을 우선하되, 모델별 가격·ZDR·출력 형식·용량 검사를 통과한 다른 공급 경로도 허용한다.
검사 결과의 `allowed_providers`만 요청의 `provider.only`에 넣고 `allow_fallbacks: true`를 사용한다.
`data_collection: deny`, `zdr: true`, `require_parameters: true`, 가격 상한은 유지한다. 적격 경로가 없으면 생성 전에 중단한다.
계정 수준의 공급자/개인정보 제한은 이 설정으로 우회하지 않는다. [OpenRouter 공급자 라우팅](https://openrouter.ai/docs/guides/routing/provider-selection).
키 한도는 로컬 테스트 예산이나 계정 잔액으로 취급하지 않는다.
2026-09-20 사전 검사에서 Flash의 일반 Vertex 경로는 비활성이고 ZDR 적격 priority 경로가 활성이라, 해당 모델의 라우팅/예약 상한은 입력/출력 100만 토큰당 $1.35/$6.75다. 실제 호출 직전에도 가격과 접근 조건을 재확인하며 과거 비용은 당시 기록대로 보존한다.

## 웹

```sh
npm run dev                     # http://127.0.0.1:3000
npm run check:web
npm run build:web
npm --prefix web run start      # 빌드한 앱 실행, 기본 localhost 전용
```

- `?questions=6..10`: 요청할 최대 문항 수. 기본 10개이며 근거가 부족하면 더 적게 생성한다.
- `?seconds=10..120`: 질문당 답변 시간. 기본 40초.
- `?demo=1` 또는 비회원: **DB에 보관된 실제 기존 결과**를 체험한다. 새 분석·답변 DB 저장은 없다. 예제 원본 PDF/이미지는 공개하지 않는다. `fast=1`은 더 이상 분석을 모사하지 않는다.
- 개발 서버에서 macOS 파일 감시 한도 `EMFILE`이 나타나면 `WATCHPACK_POLLING=1000 npm run dev` 또는 빌드 후 `start`를 사용한다.
- 질문·인용문은 줄바꿈까지 보존하며 페이지 번호는 코어가 검증한 앵커에서 가져온다.
- 회원 답변은 문항별 RPC로 저장하며 Supabase 확인 전에는 다음 문항으로 넘어가지 않는다. 문항별 답변의 기준은 DB이며 실행·결과는 `output/web/runs/<runId>/`에도 백업한다. 과거 일괄 저장의 로컬 답변 파일은 보존한다. [DB·Auth·Storage 설정](supabase/README.md). 기업 전송·답변 평가는 없다.
- 부분 결과는 목표/생성 개수와 제한 사항을 표시한 뒤 진행할 수 있다. 질문이 없으면 시작하지 않는다.
- 저장 실패 시 같은 답변만 재시도하며 분석을 다시 실행하지 않는다. 저장된 답변은 DB에서 복구하고 미제출 초안은 같은 브라우저에서 최대 24시간 복구한다. 로그아웃하면 로컬 초안을 지운다.
- 답변 API는 완료된 실행의 질문 ID·중복·문자열 길이·경과 시간을 검사한다. 동일 재전송은 허용하지만 이미 저장된 다른 답변은 덮어쓰지 않는다.
- `raw/` 원본 모델 응답, `regions/` 영역 미리보기, 결과·이벤트·답변은 해당 실행 폴더에 보관하며 Git에는 포함하지 않는다.

현재는 **단일 상시 실행 Node 서버 MVP**다. 회원 소유권·RLS·비공개 Storage·30일 보관/삭제 후 7일 정리는 구현했다. 채용 담당자는 자신이 만든 테스트의 동의한 응시 결과만 조회한다. 플랫폼 전체 관리자, 배포 도메인·메일 발송·운영 예산은 별개다. 영속 작업 큐·다중 서버 실행은 없으므로 그대로 서버리스에 배포하지 않는다.
진행 파일은 원자적으로 교체하고 기록을 순서대로 처리하며, 예산 원장 잠금으로 중복 지출을 막는다.

## CLI·현재 벤치마크

승인된 누적 한도를 넣고 항상 새로운 출력 경로를 사용한다.

```sh
npm run analyze -- example.pdf --track design --max-cost-usd 10 \
  --output result.json --guide-output questions.txt --events \
  --raw-response-dir new-raw --preview-dir new-regions

# 기존 두 문서 검증 경로. 아래는 실행 예시이며 자동 실행하지 않는다.
npm run benchmark -- run --phase pilot --run UNIQUE_RUN \
  --ids d-shuu,m-damyul --max-cost-usd 10
npm run benchmark -- report --run UNIQUE_RUN
```

`analyze:openrouter`는 같은 CLI의 호환 별칭이다. `--provider gemini`/GCP 옵션은 거부한다.
벤치마크 신규 실행은 현재 두 PDF pilot만 지원한다. 과거 결과의 읽기 전용 보고서·비교 집계는 유지한다.
다운로드한 원본 PDF는 재배포하지 않으므로 별도 준비한 로컬 자료가 필요하다.

근거/질문 단계만 좁혀 시험할 때는 기존 코어를 재사용하는 명시적 유료 명령이 있다. 같은 두 PDF만 지원하며 기본 `npm test`에서는 실행하지 않는다.

```sh
npm run test:quality:paid -- --paid --input PRIOR_RESULT.json \
  --id d-shuu --run NEW_SOURCE_RUN --stage sources --max-cost-usd 10
npm run test:quality:paid -- --paid \
  --input output/benchmark/quality-replays/NEW_SOURCE_RUN/result.json \
  --id d-shuu --run NEW_QUESTION_RUN --stage questions --max-cost-usd 10
```

선정·추출은 입력 결과대로 고정한다. 질문 재생에는 v0.16/0.17 독립 판독을 마친 근거만 허용하며 최신 무과금 근거/크롭 검사는 이전 적격성을 더 좁힐 수만 있다. `--question-max-output-tokens`는 재생 실험에 한해 출력 상한을 낮출 수 있고 manifest에 기록한다(기본 16,384). 위 명령도 잔여 최대 비용 예약이 부족하면 차단되며 기존 유보금/원장을 우회하지 않는다.

## 분석 방법·안전장치

1. PDF 형식·암호화·50 MB·1~60페이지 제한을 확인한다.
2. 문서가 길면 겹치는 청크로 목차를 읽고, 분리 PDF 페이지 번호를 원본 번호로 변환한다.
3. 프로젝트와 검증 포인트를 선정하고 중심 페이지와 필수 맥락을 함께 선택한다.
4. 페이지/타일 이미지에서 작업물·설명문·제목·차트·참고자료를 구분한다. 제목 box에 바깥 설명문을 섞거나 배치만으로 협업·전후 관계를 확정하지 않는다.
5. 기존 영역의 확대 크롭을 포인트당 최대 두 개 제공하고, 디자인의 역할·선택·수정 과정·사용/검증과 마케팅의 지표명·값·단위·기간·분모·비교 기준을 분리해 추출한다. `point_checks`에 각 포인트의 추출/판독 불가/원문 미발견을 기록하고 필수 맥락 누락을 검사한다. 명확한 주장 하나와 같은 페이지의 독립 작업물은 **검수 전에** 별개 근거로 나누되 모든 앵커·필수 맥락을 보존한다. 비교/실험/지표를 임의 분리하지 않는다. `metric_sources`의 필드별 원문만 대조하고 없는 조건은 `null`로 둔다.
6. 인용문·영역·숫자 대상·필수 맥락을 검사한 뒤 Gemini에 **기존 설명 없이 연결 크롭과 ID만** 제공해 문자/시각 관찰을 읽는다(`CropReadings`). 그 판독과 근거 후보를 별도로 대조하고, `reading_excerpt`가 자기 영역 원문/관찰에 속하는지 확인한다. `source_check.anchor_checks`가 누락·불확실·불일치이면 해당 근거는 질문에 쓰지 않는다. 텍스트 레이어 부재를 이미지 속 원문 부재로 해석하지 않는다.
7. Opus에는 선택된 검증 구절·관찰과 `context_bundle`을 전달한다. 스캔 자유문장 주제 대신 질문 방향 enum만 전달하고, 재생성 피드백도 검수자의 자유문장 대신 오류 코드만 쓴다. 원래 선정 주제는 별도 coverage 검사에 남긴다. 필수 맥락 앵커 누락을 거부하고 다른 자료의 역할을 상속하지 않는다. `listen_for`는 질문에서 실제 물은 과제의 연속 구절로 제한하며 숨은 정답 목록이 아니다. Gemini가 자기 크롭으로 본문·의도·각 가이드의 전제를 따로 검사한다. `premise_checks`는 전제별 출처, `field_text`는 검사한 문구, `experience_check`는 현재 해석/행동의 조건부/명시 행동/근거 없음 구분이다. 로컬 회귀 검사도 일반 의미 정확도를 보장하지 않는다. 같은 근거라도 실제 답이 다르면 재사용할 수 있고 의미 중복은 별도 검사한다.
8. 최대 두 생성 라운드에서 요청 개수 달성 또는 빈 후보 응답이면 종료한다. `question_target`에 요청·생성·부족 수를 남기며 부족한 질문을 템플릿으로 채우거나 분석 페이지를 자동 확대하지 않는다.

글자 없는 명확한 작업물은 텍스트 판독 불가만으로 제외하지 않는다. 텍스트 인용에는 판독 검사를 유지하고, 시각 객체에는 명확한 식별과 크롭 대조를 요구한다. 마케팅 대시보드의 독립된 수치 행과 바로 다음 지표명은 온전한 값으로 대조하며, 단위·부호·수량 대상·범위를 잘라낸 인용은 계속 거부한다. 도시/성별/연령 같은 분류 축을 분모로 넣지 않는다.

단색 배경의 텍스트 크롭 경계에 잉크가 닿으면 보류한다. 이 픽셀 검사는 OCR이 아니며 장식으로 과잉 제외되거나 복잡한 배경을 판정하지 못할 수 있다. 박스를 자동으로 늘리지 않는다. 독립 판독에서도 작은 객체 오인과 누락이 실제 관측됐다. 빈 추출은 원인 이벤트를 남기지만 자동 성공으로 보완하지 않는다. 이전 결과는 그대로 보존하며 최신 검사를 소급 통과 처리하지 않는다. 제출물에 주장이 보인다는 것과 실제 저작·성과가 증명됐다는 것은 별개다.

- 형식 오류는 최대 1회 재시도한다. 이와 별도로 각 요청의 명시적 429(HTTP 오류 또는 HTTP 200 본문 내부 오류)는 기본 30초·60초 대기로 최대 2회 재시도한다. `Retry-After`가 더 길면 해당 대기를 지키고, 120초 초과면 조기 재전송하지 않고 중단한다. 일반 네트워크/502 오류는 재시도하지 않는다. [OpenRouter Retry-After](https://openrouter.ai/docs/api_reference/errors-and-debugging#retry-after-header).
- **실패 요청의 실제 사용량·비용을 정산할 수 있어야 재시도한다.** 미확인 비용은 0원 처리하거나 유보를 자동 해제하지 않는다. 각 재시도 전에 키 잔여 한도·공유 예산을 다시 확인하고 별도 예약한다. 원본 응답의 `_application_http_attempt`, 사용량 행의 시도·대기 기록과 보고서의 `application_http_retries`로 실제 재시도 수를 남긴다. 공급자 내부 재시도 수는 계속 미확인이다.
- 세션의 유료 요청 시작 간격은 16초다. 직렬 스캔 중 관측한 상위 429를 완화하기 위한 조치이며 공식 분당 한도나 장애 해결 보장은 아니다. 대기는 비용 예약 전에 하며 취소할 수 있다.
- HTTP 오류는 안전한 공급자/오류 코드와 request/generation ID를 보존한다. 원문을 포함할 수 있는 중첩 오류 메시지는 저장하지 않는다. 과거 502에서 버려진 상세 정보를 소급 복구할 수는 없다.
- 호출 전 최대 컨텍스트·출력 비용을 보수 예약한다. 이것을 실제 입력 토큰 사용량으로 보고하지 않는다.
- API가 보고한 입력·출력·추론 토큰과 비용을 기록한다. 추론은 출력에 포함돼 중복 합산하지 않으며 미확인 값은 추정하지 않는다.
- 비용 미확인·잔액 부족·가격/모델 조건 변경은 이후 호출을 막는다. 과거 미확인 유보금은 승인된 기록 그대로 유지한다.
- JSON/Zod 통과 및 동일 모델 재검수는 독립적인 정확도 증명이 아니다.

## 검증 상태

schema 0.17 구현. 최종 코어 **158/158**, 양쪽 타입 검사·프로덕션 빌드·diff 검사 통과. 만료 정리는 예제 보호·파일 삭제 실패 시 DB/로컬 보존·성공 순서를 검사한다. 무과금 Playwright **22/22**(1440×1000/390×844): 실제 개수 2·7·9·10, 저장 ACK/지연/실패/재시도, 새로고침 초안 복구, 이메일 UI, 코딩 입력, 0문항/실패 차단, 모바일 화면 전환 스크롤. 합성 응답 검사와 실제 Supabase 검사를 구분한다. 최종 빌드의 브라우저 JS 23개에 실제 서버 키가 포함되지 않은 것도 확인했다.

실제 Supabase에서는 확인된 임시 계정으로 **이메일·비밀번호 로그인, 다른 계정의 실행/답변/원문 접근 차단, 비공개 파일 링크, 답변 6개 저장·동일 재전송, 새로고침 복구, 내 기록, 로그아웃, 비회원 마케팅 9개 예제**를 검증했다. 모델 호출 0회. 임시 Auth 계정·합성 실행/파일은 정리했다. 회원가입 인증 메일·비밀번호 재설정 메일의 실제 수신/링크 완료는 **사용자 요청으로 미검증·보류**다. migration/RPC/권한은 로컬 PostgreSQL WASM에서도 검증했다. 모델 자동 통과와 Codex 원문 검수(사람 전문가 아님)는 별도다.

과거 0.17.1 전체 CLI 실행(이하 비용·차단 상태는 당시 별도 원장 기록이며, 최신 재시험 상태는 문서 상단 참조):

| 분야 | 자동 최종 질문 | 별도 엄격 통과 | 전체 분석 시간 | 실제 API 비용 |
| --- | ---: | ---: | ---: | ---: |
| 디자인 (`mvp-v017-02`) | 9 | 4 | 490.213초 | $0.70678005 |
| 마케팅 (`mvp-v017-02`) | 3 | 2 | 451.300초 | $0.80470440 |

두 문서 모두 빠진 핵심 포인트와 최종 오류가 있어 **최소 MVP·10문항 목표 미달**이다. 이후 수정으로 위 결과를 소급 합격시키지 않는다. 두 CLI 시험 $2.48270885와 웹 추출 실패 $0.31553990의 확인 합계는 **$2.79824875**다. 마지막 웹 HTTP 400의 토큰·비용은 미확인이다. 기존 유보 $1.634304와 신규 미정산 예약 $1.4708736을 유지한다. 계산상 잔여 $5.73087765가 있어도 **blocked=true이므로 추가 호출 불가**다. 사용자 요청 없이 유보 전환·해제·새 원장·재시험을 하지 않는다. [실행별 토큰·시간·비용·원인](benchmark/MVP_VALIDATION_REPORT.md), [v0.16 부분 시험 이력](benchmark/QUALITY_IMPROVEMENT_REPORT.md).

20개 전체나 모델 일반 성능으로 확대하지 않는다. 원본·실패 결과·비용 원장은 보존하며 이번 변경의 커밋·푸시·병합은 별도 요청 때 수행한다.

```sh
npm run check
npm test
npm run check:web
npm run build:web
npm run test:e2e                 # Chromium 데스크톱 + 모바일, 유료 호출 없음
```

현재 회귀 테스트는 기존 근거·질문·PDF 좌표 검증과 OpenRouter 비용 안전장치, UI 어댑터·실행 인자 검사를 포함한다.
삭제한 직접 Gemini/GCP 전송 경로 전용 테스트는 제거했으며 새 UI 테스트로 대체했다.
저장된 두 문서 결과를 UI에 연결하는 검사는 새 질문 생성이나 새로운 품질 합격으로 집계하지 않는다.

무료 UI 검사는 1440×1000/390×844에서 합성 10문항 전체 답변, 부분 결과 경고, 0문항/분석 실패 차단, 저장 지연·실패·답변만 재시도를 다룬다. 실제 로컬 파일 저장과 동일/동시 재전송은 별도 합성 완료 실행으로 코어 테스트에서 검사한다. 실제 원본 PDF의 질문 품질·답변 저장 성공은 유료 웹 실행 기록과 구분한다.

### 반복 가능한 Playwright 검사

```sh
npm ci --prefix web
npm --prefix web exec -- playwright install chromium   # 최초 한 번
npm run test:e2e
```

`web/`에 Playwright Test 1.63.0을 고정했다. [공식 테스트 서버 설정](https://playwright.dev/docs/test-webserver)을 사용해
프로덕션 빌드 후 `127.0.0.1:3101`에서 전용 서버를 시작하고 종료한다. 테스트는 `.next-e2e`, 미리보기는 `.next`로 분리한다. 무료·유료 E2E끼리는 같은 테스트 빌드를 사용하므로 동시에 실행하지 않는다.
기존 서버를 재사용하지 않으며 키는 빈 값, 승인 예산은 0으로 고정한다. 외부 Browser 플러그인이 없어 Playwright를 직접 사용했다.

- 두 직군 × 데스크톱/모바일: 업로드 → running/complete 폴링 → 다중행 질문/인용문/페이지 표시 → 모든 답변 전송 → 완료.
- 실제 API 방어: 6~10 밖의 요청/짧은 PDF 400, 외부 Origin 403, Auth 미설정/비회원 차단. 실제 연결된 미리보기에서는 예산 0으로 새 분석이 차단되는 것도 확인했다.
- 정상 흐름의 콘솔 오류/경고·가로 넘침·오류 오버레이를 검사한다. 분석 응답/답변 저장은 **합성 데이터와 API stub**이므로 모델 품질·실제 PDF 분석·실제 디스크 저장의 E2E 증거가 아니다.
- 스크린샷/실패 trace는 OS 임시 폴더의 `proofolio-playwright-results/`에만 저장한다. 원본 포트폴리오나 비밀키 없이 재실행할 수 있다.

2026-09-20 **v0.15 당시**: **10/10 UI 테스트**, **123/123 코어 테스트**, 양쪽 타입 검사와 프로덕션 빌드 통과. 첫 샌드박스 실행은 Chromium macOS MachPort 권한에서 앱 진입 전 실패했고, 승인된 환경의 재실행과 최종 재검사는 통과했다. 이전 중복 `.next` 선언 오류도 정상 빌드로 생성 캐시를 갱신한 뒤 해소됐으며 소스 파일은 삭제하지 않았다.
실행기 자체의 `NO_COLOR`/`FORCE_COLOR` 충돌 경고는 있었지만 정상 흐름의 브라우저 콘솔 오류/경고는 없었다.

### 명시적 유료 웹 E2E (기본 테스트와 분리)

두 원본 PDF와 **이메일 인증이 끝난 운영자 테스트 계정**이 있고 별도 실행 승인이 있을 때만 실행한다. `PROOFOLIO_E2E_EMAIL/PASSWORD`를 프로세스 환경변수로 제공한다(로그/Git/스크린샷에 비밀번호를 넣지 않는다). 디자인 데스크톱·마케팅 모바일에서 각각 한 번 업로드한다. 기본 무과금 명령에는 포함되지 않으며 **이번 회원 연동 후 유료 PDF E2E는 재실행하지 않았다**.

```sh
PROOFOLIO_PAID_E2E=1 PROOFOLIO_MAX_COST_USD=10 PROOFOLIO_E2E_RUN=NEW_UNIQUE_RUN_ID \
  npm run test:e2e:paid
```

- `127.0.0.1:3102` 전용 서버, worker 1·자동 재시도 0. 실패 후 자동 재업로드하지 않는다.
- 실행 ID는 소문자 영문·숫자·하이픈만 사용한다. `web-ten-01`~`web-ten-05`는 사용 완료 경로이며 덮어쓰지 않는다. `PROOFOLIO_E2E_DOCUMENT=d-shuu` 또는 `m-damyul`은 별도 승인한 단일 문서 시험에만 사용한다. 미설정 시 두 문서이며 마케팅부터 진행한다. 실패 후 자동 재업로드하지 않는다.
- 코드/프롬프트/웹/모델 설정/PDF 해시, 무료 모델·공급자·가격·ZDR 검사, 기존 원장 전후 상태를 고정·기록한다.
- 기존 `output/openrouter-budget.jsonl`만 사용한다. 이 과거 원장은 차단 상태이며 기존 미확인 비용을 승인 없이 해제하거나 새 원장으로 우회하지 않는다. 테스트 도구를 갱신한 것과 유료 운영이 승인된 것은 다르다.
- 실제 웹 실행 결과를 `output/benchmark/runs/<run>/` 보고서 입력으로 복사한다. 별도 CLI 유료 재분석은 하지 않는다. 답변은 명확히 표시된 **자동 E2E 합성 답변**이며 후보자 답변 평가가 아니다.
- 원문과 전체 카드를 별도로 검수해 `source-audit.json`을 작성한 후 `npm run benchmark -- report --run <run>`으로 기존 집계기를 재사용한다.
- 최소 MVP(문서별 3개·설명 질문 2개·핵심 포인트 충족·추가 전제/오연결 0)와 **10문항 목표**는 별도로 보고한다.
- 종료하면 유료 서버가 닫힌다. 미리보기는 `OPENROUTER_API_KEY= PROOFOLIO_MAX_COST_USD=0 npm --prefix web run start -- --port 3100`으로 복귀한다.

## 남은 일

1. **플랫폼 전체 관리자(별도 범위)**: 채용 담당자 UI는 본인 테스트만 조회한다. 모든 회원/실행/질문/답변을 관리하려면 별도의 서버 관리자 인가가 필요하다.
2. **메일 운영**: 사용자가 미룬 SMTP/발신자 설정, 실제 가입 인증 및 비밀번호 재설정 링크 수신을 확인한다. 이메일 확인을 끄는 우회는 하지 않았다.
3. **배포 운영**: 단일 상시 Node 서버·영속 디스크, 정확한 HTTPS Auth URL, 운영용 승인 예산/원장·모니터링을 설정한다. 현재 localhost 미리보기는 예산 0이다. 서버리스/다중 인스턴스가 필요하면 영속 작업 큐·분산 비용 제한·독립 정리 작업을 먼저 도입한다.

사용자가 현재 질문 수준을 수용했으므로 이번 범위에서 추가 PDF 재시험·20개 확장·음성·답변 평가를 하지 않았다. 품질 한계는 기존 보고서에 남기며 DB 시딩을 새 품질 통과로 집계하지 않는다.

기존 Next.js 하위 PostCSS 취약 의존성은 `postcss` **8.5.28 override**로 갱신했고 설치 audit는 **0개**였다(2026-09-20 스냅샷, 지속 보안 보장 아님).

## 파일

- `src/pipeline.ts`: `analyzePdf(pdfBytes, options)`, 페이지 선정·근거 추출·검수.
- `src/questions.ts`: 질문 생성·출처/중복/핵심 포함 검사.
- `src/coding.ts`, `coding-cli.ts`: 공개 저장소 입력·한 번의 코드 질문 생성·원문 연결.
- `src/openrouter.ts`: 유일한 모델 통신 경로.
- `src/llm.ts`: 공용 요청 타입·안전한 HTTP 처리·누적 예산. 과거 비용 읽기 함수는 네트워크를 사용하지 않는다.
- `src/env.ts`: 서버 전용 환경변수 허용 목록·공유 예산 설정.
- `web/`: PR #1 UI와 현재 코어 실행 어댑터.
- `supabase/`: 회원·실행·질문·답변·예제 migration, SQL 검사와 무과금 예제 가져오기.
- `benchmark/`: 자료 목록·원문 기준표·검증 보고서. 이전 freeze/보고서는 읽기 전용 이력이다.
- `output/`: Git 제외 원본·실행 결과·비용 원장.

[최신 MVP 개선·중단 보고](benchmark/MVP_VALIDATION_REPORT.md) · [이전 단계별 품질 개선](benchmark/QUALITY_IMPROVEMENT_REPORT.md) · [이전 10문항·웹 실행](benchmark/TEN_QUESTION_WEB_REPORT.md) · [이전 OpenRouter 질문 품질](benchmark/OPENROUTER_PILOT_REPORT.md) · [이전 MVP 검증](benchmark/MVP_REPORT.md) ·
[과거 모델 비교](benchmark/MODEL_COMPARISON.md) · [작업 이력](CHECKPOINT.md)
